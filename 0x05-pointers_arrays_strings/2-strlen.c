#include "holberton.h"

/**
 * _strlen - Returns the length of a string
 * @s: String to count
 *
 * Return: String length
 */
int _strlen(char *s)
{
	char *s;
	int x;

	for (x = 0; *s != '\0'; s++);

	return 0;
}
